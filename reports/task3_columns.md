# Task 3 — Inspect the columns (features.*)

From the feature table (Excel screenshot):
- **Target (y):** `is_high_value`
- **ID passthrough:** `user_id`
- **Features (X):** `country`, `n_orders`, `avg_amount`, `total_amount`

**Checkpoint:** y = `is_high_value`, ID = `user_id`, X = (`country`, `n_orders`, `avg_amount`, `total_amount`)
